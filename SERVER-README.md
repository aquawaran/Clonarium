# 🚀 Запуск многопользовательской Clone

## 📋 Что нужно установить

### Node.js и npm
```bash
# Установите Node.js с сайта https://nodejs.org/
# Проверьте установку:
node --version
npm --version
```

## 🔧 Установка зависимостей

1. Откройте терминал в папке `Clone`
2. Установите все пакеты:
```bash
npm install
```

## 🚀 Запуск сервера

### Для разработки:
```bash
npm run dev
```

### Для продакшена:
```bash
npm start
```

## 📱 Доступ к приложению

Откройте в браузере:
```
http://localhost:3000
```

## 🌐 Многопользовательские функции

### ✨ Что работает в реальном времени:
- **Новые посты** - появляются у всех пользователей мгновенно
- **Реакции** - обновляются в реальном времени
- **Комментарии** - появляются мгновенно
- **Уведомления** - приходят мгновенно

### 👥 Социальные функции:
- **Подписки** на пользователей
- **Персональная лента** с постами подписанных
- **Поиск пользователей**
- **Загрузка аватаров и медиа**
- **Профили пользователей**

### 📁 Структура файлов на сервере:
```
Clone/
├── uploads/           # Загруженные файлы
│   └── [user_id]/    # Папки пользователей
├── server.js          # Сервер с API
├── script-server.js  # Frontend с API
├── index.html        # Главный файл
├── styles.css        # Стили
└── package.json      # Зависимости
```

## 🔌 API Endpoints

### Аутентификация:
- `POST /api/register` - Регистрация
- `POST /api/login` - Вход
- `GET /api/me` - Текущий пользователь

### Профиль:
- `PUT /api/profile` - Обновление профиля
- `POST /api/avatar` - Загрузка аватара
- `DELETE /api/account` - Удаление аккаунта

### Посты:
- `GET /api/feed` - Лента новостей
- `POST /api/posts` - Создание поста
- `POST /api/posts/:id/reactions` - Реакции
- `POST /api/posts/:id/comments` - Комментарии

### Пользователи:
- `GET /api/users/search` - Поиск пользователей
- `POST /api/users/:id/follow` - Подписки

### Уведомления:
- `GET /api/notifications` - Получение уведомлений

## 🔄 WebSocket события

### Клиент → Сервер:
- `authenticate` - Аутентификация WebSocket

### Сервер → Клиент:
- `new_post` - Новый пост
- `post_reaction` - Обновление реакций
- `new_comment` - Новый комментарий
- `notification` - Уведомление

## 🛡️ Безопасность

- **JWT токены** для аутентификации
- **BCrypt** для хеширования паролей
- **Rate limiting** для защиты от DDOS
- **Helmet** для безопасности HTTP
- **Валидация файлов** для загрузки

## 📱 Мобильная версия

Полностью адаптивный дизайн для всех устройств:
- Смартфоны
- Планшеты
- Ноутбуки
- Десктопы

## 🔧 Конфигурация

### Переменные окружения:
```bash
PORT=3000
JWT_SECRET=your-secret-key
```

## 🚀 Развертывание

### Для продакшена:
1. Установите PM2:
```bash
npm install -g pm2
```

2. Запустите сервер:
```bash
pm2 start server.js --name "clone-social"
```

3. Настройте Nginx для SSL (опционально)

## 📊 Мониторинг

Сервер логирует:
- Подключения пользователей
- Создание постов
- Реакции и комментарии
- Ошибки API

## 🎯 Особенности

- **Real-time** обновления через Socket.IO
- **Многопользовательская** база данных в памяти
- **Файловое хранилище** для медиа
- **Адаптивный** дизайн
- **Безопасная** аутентификация

---

**Готово к использованию! 🎉**

Запустите сервер и откройте http://localhost:3000 в нескольких браузерах для тестирования многопользовательских функций!
